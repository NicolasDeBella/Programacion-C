/*
Ejercicio 5-2:
Pedir el ingreso de 10 números enteros entre -1000 y 1000. Determinar:
Cantidad de positivos y negativos.
Sumatoria de los pares.
El mayor de los impares.
Listado de los números ingresados.
Listado de los números pares.
Listado de los números de las posiciones impares.
Los números que se repiten
Los positivos creciente y los negativos de manera decreciente
Se deberán utilizar funciones y vectores.

 */

#include <stdio.h>
#include <stdlib.h>
int inicializarArray (int array[], int len, int numero);

int main(void) {
	setbuf(stdout, NULL);
	int arrayNumeros;
	int resultado;
	int numeroIngresado;
	inicializarArray(arrayNumeros[],10, numeroIngresado);


	return EXIT_SUCCESS;
}

void inicializarArray (int array[], int len, int numero)
{
	int i=0;

	if(array!=NULL && len>0)
	{
		for(i=0;i<len;i++)
		{
			array[i]=numero;
		}

	}

}
